rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {

    /* ================= Helpers ================= */

    function isSignedIn() {
      return request.auth != null;
    }

    function hasUserDoc() {
      return isSignedIn()
        && firestore.exists(
          /databases/(default)/documents/users/$(request.auth.uid)
        );
    }

    function userDoc() {
      return firestore.get(
        /databases/(default)/documents/users/$(request.auth.uid)
      );
    }

    function userHasRole(roleName) {
      return hasUserDoc()
        && (
          (
            userDoc().data.keys().hasAny(["role"])
            && userDoc().data.role == roleName
          )
          ||
          (
            userDoc().data.keys().hasAny(["primaryRole"])
            && userDoc().data.primaryRole == roleName
          )
          ||
          (
            userDoc().data.keys().hasAny(["roles"])
            && userDoc().data.roles is list
            && roleName in userDoc().data.roles
          )
        );
    }

    function isRoot() {
      return userHasRole("root");
    }

    function userInmobiliarias() {
      return hasUserDoc()
        && userDoc().data.keys().hasAny(["inmobiliarias"])
        && userDoc().data.inmobiliarias is list
        ? userDoc().data.inmobiliarias
        : [];
    }

    function belongsToUserInmobiliaria(inmobiliariaId) {
      return isSignedIn()
        && inmobiliariaId in userInmobiliarias();
    }

    function isImage() {
      return request.resource != null
        && request.resource.contentType.matches('image/.*');
    }

    function isValidImageSize() {
      return request.resource != null
        && request.resource.size <= 10 * 1024 * 1024;
    }

    function isValidProfileImageSize() {
      return request.resource != null
        && request.resource.size <= 5 * 1024 * 1024;
    }

    /* ================= Perfil público de usuario ================= */

    match /public-profiles/{userId}/{allPaths=**} {
      allow read: if true;

      allow create, update: if
        isSignedIn()
        && request.auth.uid == userId
        && isImage()
        && isValidProfileImageSize();

      allow delete: if isSignedIn() && request.auth.uid == userId;
    }

    function isValidInstagramReel() {
      return request.resource != null
        && request.resource.contentType.matches('^video/(mp4|quicktime)$')
        && request.resource.size <= 300 * 1024 * 1024;
    }
    
    function isValidParticularRequestImage() {
      return request.resource != null
        && request.resource.contentType.matches('image/.*')
        && request.resource.size <= 10 * 1024 * 1024;
    }

    function isValidVerificationFile() {
      return request.resource != null
        && request.resource.size < 10 * 1024 * 1024
        && request.resource.contentType.matches(
          '^(application/pdf|image/jpeg|image/png)$'
        );
    }

    function isValidBillingProof() {
      return request.resource != null
        && request.resource.size <= 10 * 1024 * 1024
        && request.resource.contentType.matches(
          '^(application/pdf|image/.*)$'
        );
    }

    function isAgencyAdmin(inmobiliariaId) {
      return isRoot()
        || (userHasRole("admin") && belongsToUserInmobiliaria(inmobiliariaId));
    }

    function hasVerifiedAuthEmail() {
      return isSignedIn()
        && request.auth.token.email_verified == true
        && request.auth.token.email is string;
    }

    function hasConsortiumPortalAccess(inmobiliariaId, consortiumId) {
      return hasVerifiedAuthEmail()
        && firestore.exists(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominiums/$(consortiumId)
        )
        && firestore.get(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominiums/$(consortiumId)
        ).data.portalEmails is list
        && request.auth.token.email in firestore.get(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominiums/$(consortiumId)
        ).data.portalEmails;
    }

    function hasConsortiumOwnerPortalAccess(inmobiliariaId, consortiumId) {
      return hasVerifiedAuthEmail()
        && firestore.exists(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominiums/$(consortiumId)
        )
        && firestore.get(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominiums/$(consortiumId)
        ).data.ownerPortalEmails is list
        && request.auth.token.email in firestore.get(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominiums/$(consortiumId)
        ).data.ownerPortalEmails;
    }

    function hasConsortiumPortalResourceAccess(inmobiliariaId, consortiumId, sectionId, resourceId) {
      return firestore.exists(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_portal_resources/$(resourceId)
        )
        && firestore.get(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_portal_resources/$(resourceId)
        ).data.published == true
        && firestore.get(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_portal_resources/$(resourceId)
        ).data.consortiumId == consortiumId
        && firestore.get(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_portal_resources/$(resourceId)
        ).data.section == sectionId
        && firestore.exists(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_portal_settings/$(consortiumId)
        )
        && firestore.get(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_portal_resources/$(resourceId)
        ).data.section in firestore.get(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_portal_settings/$(consortiumId)
        ).data.sections
        && firestore.get(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_portal_settings/$(consortiumId)
        ).data.sections[firestore.get(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_portal_resources/$(resourceId)
        ).data.section].enabled == true
        && (
          (
            firestore.get(
              /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_portal_resources/$(resourceId)
            ).data.visibility == "all"
            && hasConsortiumPortalAccess(inmobiliariaId, consortiumId)
          )
          || (
            firestore.get(
              /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_portal_resources/$(resourceId)
            ).data.visibility == "owners"
            && hasConsortiumOwnerPortalAccess(inmobiliariaId, consortiumId)
          )
        );
    }

    function hasConsortiumUnitPortalAccess(inmobiliariaId, unitId) {
      return hasVerifiedAuthEmail()
        && firestore.exists(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_units/$(unitId)
        )
        && firestore.get(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_units/$(unitId)
        ).data.portalEmails is list
        && request.auth.token.email in firestore.get(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_units/$(unitId)
        ).data.portalEmails;
    }

    function isValidConsortiumDocument() {
      return request.resource != null
        && request.resource.size > 0
        && request.resource.size <= 10 * 1024 * 1024
        && request.resource.contentType.matches(
          '^(application/pdf|image/jpeg|image/png|image/webp)$'
        );
    }

    /* ================= Inmobiliarias - documentación privada ================= */

    match /inmobiliarias/{inmobiliariaId}/verificacion/{userId}/{fileName} {

      allow read: if
        isRoot()
        || belongsToUserInmobiliaria(inmobiliariaId);

      allow create, update: if
        isSignedIn()
        && request.auth.uid == userId
        && belongsToUserInmobiliaria(inmobiliariaId)
        && isValidVerificationFile();

      allow delete: if
        isRoot()
        || (
          isSignedIn()
          && request.auth.uid == userId
          && belongsToUserInmobiliaria(inmobiliariaId)
        );
    }

    /* ================= Inmobiliarias - branding público ================= */

    match /inmobiliarias/{inmobiliariaId}/branding/{allPaths=**} {

      allow read: if true;

      allow create, update: if
        isSignedIn()
        && belongsToUserInmobiliaria(inmobiliariaId)
        && isImage()
        && isValidImageSize();

      allow delete: if
        isRoot()
        || belongsToUserInmobiliaria(inmobiliariaId);
    }

    match /inmobiliarias/{inmobiliariaId}/logo/{allPaths=**} {

      allow read: if true;

      allow create, update: if
        isSignedIn()
        && belongsToUserInmobiliaria(inmobiliariaId)
        && isImage()
        && isValidImageSize();

      allow delete: if
        isRoot()
        || belongsToUserInmobiliaria(inmobiliariaId);
    }

    match /inmobiliarias/{inmobiliariaId}/backgrounds/{allPaths=**} {

      allow read: if true;

      allow create, update: if
        isSignedIn()
        && belongsToUserInmobiliaria(inmobiliariaId)
        && isImage()
        && isValidImageSize();

      allow delete: if
        isRoot()
        || belongsToUserInmobiliaria(inmobiliariaId);
    }

    match /inmobiliarias/{inmobiliariaId}/{fileName} {

      allow read: if true;

      allow create, update: if
        isSignedIn()
        && belongsToUserInmobiliaria(inmobiliariaId)
        && isImage()
        && isValidImageSize();

      allow delete: if
        isRoot()
        || belongsToUserInmobiliaria(inmobiliariaId);
    }

    /* ================= Inmuebles ================= */

    match /inmuebles/{inmobiliariaId}/{inmuebleId}/{fileName} {

      allow read: if true;

      allow create, update: if
  isSignedIn()
  && (
    isRoot()
    || belongsToUserInmobiliaria(inmobiliariaId)
  )
  && isImage()
  && isValidImageSize();

      allow delete: if
        isRoot()
        || belongsToUserInmobiliaria(inmobiliariaId);
    }

    /* ================= Emprendimientos ================= */

    match /emprendimientos/{inmobiliariaId}/{emprendimientoId}/{fileName} {

      allow read: if true;

      allow create, update: if
        isSignedIn()
        && (
          isRoot()
          || belongsToUserInmobiliaria(inmobiliariaId)
        )
        && isImage()
        && isValidImageSize();

      allow delete: if
        isRoot()
        || belongsToUserInmobiliaria(inmobiliariaId);
    }

    match /inmuebles/{inmobiliariaId}/{inmuebleId}/instagram/{fileName} {

      allow read: if true;

      allow create: if
        isSignedIn()
        && (
          isRoot()
          || belongsToUserInmobiliaria(inmobiliariaId)
        )
        && isValidInstagramReel();

      allow update: if false;

      allow delete: if
        isRoot()
        || belongsToUserInmobiliaria(inmobiliariaId);
    }

    /* ================= Solicitudes particulares - imágenes privadas =================
       Ruta:
       /particular_publication_requests/{requestId}/{userId}/{fileName}
    */

    match /particular_publication_requests/{requestId}/{userId}/{fileName} {
    
      allow get: if isSignedIn();

      allow list: if false;

      allow create: if
        isSignedIn()
        && request.auth.uid == userId
        && isValidParticularRequestImage();

     allow update: if false;

     allow delete: if
       isRoot()
       || (
         isSignedIn()
         && request.auth.uid == userId
        );
    }

    /* ================= Cuenta corriente - comprobantes privados ================= */

    match /billing/{inmobiliariaId}/payment-proofs/{reportId}/{fileName} {
      allow read: if
        isRoot()
        || belongsToUserInmobiliaria(inmobiliariaId);

      allow create: if
        (
          isRoot()
          || belongsToUserInmobiliaria(inmobiliariaId)
        )
        && isValidBillingProof();

      allow update: if false;

      allow delete: if
        isRoot()
        || (
          belongsToUserInmobiliaria(inmobiliariaId)
          && !firestore.exists(
            /databases/(default)/documents/billing_payment_reports/$(reportId)
          )
        );
    }

    /* ================= Consorcios - documentación privada ================= */

    match /consorcios/{inmobiliariaId}/{consortiumId}/expenses/{periodId}/{expenseId}/{documentId}/{fileName} {
      allow read: if
        isAgencyAdmin(inmobiliariaId)
        || hasConsortiumPortalAccess(inmobiliariaId, consortiumId);

      allow create: if
        isAgencyAdmin(inmobiliariaId)
        && isValidConsortiumDocument();

      allow update: if false;

      allow delete: if
        isAgencyAdmin(inmobiliariaId)
        && !firestore.exists(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_expense_documents/$(documentId)
        );
    }

    match /consorcios/{inmobiliariaId}/{consortiumId}/supplier-obligations/{obligationId}/{fileName} {
      allow read: if isAgencyAdmin(inmobiliariaId);

      allow create: if
        isAgencyAdmin(inmobiliariaId)
        && isValidConsortiumDocument();

      allow update: if false;

      allow delete: if
        isAgencyAdmin(inmobiliariaId)
        && !firestore.exists(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_supplier_obligations/$(obligationId)
        );
    }

    match /consorcios/{inmobiliariaId}/{consortiumId}/supplier-payments/{paymentId}/{fileName} {
      allow read: if isAgencyAdmin(inmobiliariaId);

      allow create: if
        isAgencyAdmin(inmobiliariaId)
        && isValidConsortiumDocument();

      allow update: if false;

      allow delete: if
        isAgencyAdmin(inmobiliariaId)
        && !firestore.exists(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_supplier_payments/$(paymentId)
        );
    }

    match /consorcios/{inmobiliariaId}/{consortiumId}/treasury-movements/{movementId}/{fileName} {
      allow read: if isAgencyAdmin(inmobiliariaId);

      allow create: if
        isAgencyAdmin(inmobiliariaId)
        && isValidConsortiumDocument();

      allow update: if false;

      allow delete: if
        isAgencyAdmin(inmobiliariaId)
        && !firestore.exists(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_treasury_movements/$(movementId)
        );
    }

    match /consorcios/{inmobiliariaId}/{consortiumId}/treasury-reconciliations/{reconciliationId}/{fileName} {
      allow read: if isAgencyAdmin(inmobiliariaId);

      allow create: if
        isAgencyAdmin(inmobiliariaId)
        && isValidConsortiumDocument();

      allow update: if false;

      allow delete: if
        isAgencyAdmin(inmobiliariaId)
        && !firestore.exists(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_treasury_reconciliations/$(reconciliationId)
        );
    }

    match /consorcios/{inmobiliariaId}/{consortiumId}/claims/{unitId}/{claimId}/initial/{fileName} {
      allow read: if
        isAgencyAdmin(inmobiliariaId)
        || hasConsortiumUnitPortalAccess(inmobiliariaId, unitId);

      allow create: if
        (
          isAgencyAdmin(inmobiliariaId)
          || hasConsortiumUnitPortalAccess(inmobiliariaId, unitId)
        )
        && isValidConsortiumDocument();

      allow update: if false;

      allow delete: if
        isAgencyAdmin(inmobiliariaId)
        && !firestore.exists(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_claims/$(claimId)
        );
    }

    match /consorcios/{inmobiliariaId}/{consortiumId}/portal/{sectionId}/{resourceId}/{fileName} {
      allow read: if
        isAgencyAdmin(inmobiliariaId)
        || hasConsortiumPortalResourceAccess(
          inmobiliariaId,
          consortiumId,
          sectionId,
          resourceId
        );

      allow create: if
        isAgencyAdmin(inmobiliariaId)
        && isValidConsortiumDocument();

      allow update: if false;

      allow delete: if isAgencyAdmin(inmobiliariaId);
    }

    match /consorcios/{inmobiliariaId}/{consortiumId}/claims/{unitId}/{claimId}/events/{eventId}/{fileName} {
      allow read: if
        isAgencyAdmin(inmobiliariaId)
        || hasConsortiumUnitPortalAccess(inmobiliariaId, unitId);

      allow create: if
        (
          isAgencyAdmin(inmobiliariaId)
          || hasConsortiumUnitPortalAccess(inmobiliariaId, unitId)
        )
        && isValidConsortiumDocument();

      allow update: if false;

      allow delete: if
        (
          isAgencyAdmin(inmobiliariaId)
          || hasConsortiumUnitPortalAccess(inmobiliariaId, unitId)
        )
        && !firestore.exists(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_claim_events/$(eventId)
        );
    }

    match /consorcios/{inmobiliariaId}/{consortiumId}/payment-reports/{unitId}/{reportId}/{userId}/{fileName} {
      allow read: if
        isAgencyAdmin(inmobiliariaId)
        || hasConsortiumUnitPortalAccess(inmobiliariaId, unitId);

      allow create: if
        isSignedIn()
        && request.auth.uid == userId
        && hasConsortiumUnitPortalAccess(inmobiliariaId, unitId)
        && isValidConsortiumDocument();

      allow update: if false;

      allow delete: if
        isSignedIn()
        && request.auth.uid == userId
        && hasConsortiumUnitPortalAccess(inmobiliariaId, unitId)
        && !firestore.exists(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_payment_reports/$(reportId)
        );
    }

    match /consorcios/{inmobiliariaId}/{consortiumId}/penalties/{unitId}/{penaltyId}/{fileName} {
      allow read: if
        isAgencyAdmin(inmobiliariaId)
        || (
          hasConsortiumUnitPortalAccess(inmobiliariaId, unitId)
          && firestore.exists(
            /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_penalties/$(penaltyId)
          )
          && firestore.get(
            /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_penalties/$(penaltyId)
          ).data.portalVisible == true
        );

      allow create: if
        isAgencyAdmin(inmobiliariaId)
        && isValidConsortiumDocument();

      allow update: if false;

      allow delete: if
        isAgencyAdmin(inmobiliariaId)
        && !firestore.exists(
          /databases/(default)/documents/inmobiliarias/$(inmobiliariaId)/condominium_penalties/$(penaltyId)
        );
    }

    /* ================= Fallback ================= */

    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
